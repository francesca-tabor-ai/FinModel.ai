# Required for AI insights and simulations. Get a key at https://ai.google.dev/
GEMINI_API_KEY=

# Optional: server port (default 3000). Railway sets PORT automatically.
# PORT=3000

# PostgreSQL (production / Railway). When set, app uses Postgres instead of SQLite.
# Railway: add Postgres plugin, then set DATABASE_PUBLIC_URL (or DATABASE_URL) in your service variables.
# DATABASE_PUBLIC_URL=postgresql://user:password@gondola.proxy.rlwy.net:13656/railway
# DATABASE_URL=postgresql://user:password@host:5432/railway

# Optional: SQLite database path when DATABASE_URL is not set (local dev)
# DATABASE_PATH=finmodel.db

# Required in production: secret for signing session cookies (app will not start without it when NODE_ENV=production)
# SESSION_SECRET=your-secret

# Optional: if your frontend is served from a different origin, set this (e.g. https://app.example.com)
# CORS_ORIGIN=https://app.example.com
