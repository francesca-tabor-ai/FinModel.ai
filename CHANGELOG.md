# Changelog

All notable changes to FinModel.ai are documented here.

## [1.0.0] - Checkpoint (Phase 8)

### Added
- Initial release checkpoint.
- Core: dashboard, financial model view, simulations, AI agents, decision log.
- Server-side Gemini (insights, simulate); API key never exposed to client.
- SQLite schema: financial_data, decisions, agent_logs.
- Express REST API; production build and start scripts.
- README, TODO (phase reference), .env.example, .gitignore.

### Release checklist (final checkpoint)
- [x] Version set to 1.0.0 in package.json
- [x] CHANGELOG created
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] Optional: `git tag v1.0.0` for release
